# Copy this to .env.local for local dev, or set as Railway env vars.
# This is the ONLY env var the dashboard frontend needs — it just
# needs to know where the bot's Express API lives.

NEXT_PUBLIC_API_URL=https://noobnoob81-bot-production.up.railway.app
